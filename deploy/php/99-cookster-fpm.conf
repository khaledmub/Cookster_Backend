; Cookster API pool — 4 vCPU / 15 GiB shared with MariaDB + 4 transcode workers.
; Target: ~16 concurrent API requests without starving ffmpeg workers.

[www]
pm = dynamic
pm.max_children = 16
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 8
pm.max_requests = 500
