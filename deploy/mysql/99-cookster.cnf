[mysqld]
# ~25% of RAM on a 15 GiB app+DB box; leaves headroom for PHP/ffmpeg.
innodb_buffer_pool_size = 4G
innodb_buffer_pool_instances = 4
innodb_log_file_size = 512M
max_connections = 200
table_open_cache = 4000
tmp_table_size = 64M
max_heap_table_size = 64M
