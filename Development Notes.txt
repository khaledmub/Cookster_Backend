- Blocked users: adds in blocked_users table, removes from followers list, removes saved videos of this user.
-------------------------------------------------------------------------------------------------------------------------------

- Account deletion: it soft deletes the user by setting the is_soft_delete bit to 1 in DB and for sign in with Google/Apple, it soft deletes by setting this bit to 1 and also overwriting the email to "sd_id@cookster.com" and saving original email in another column named "sd_email"
-------------------------------------------------------------------------------------------------------------------------------